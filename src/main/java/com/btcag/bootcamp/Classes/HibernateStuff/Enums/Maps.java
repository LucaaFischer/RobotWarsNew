package com.btcag.bootcamp.Classes.HibernateStuff.Enums;

import com.btcag.bootcamp.Classes.Map.Model.Map;

public enum Maps {
    MAP_ONE(1, new Map()),
    MAP_TWO(2, new Map());

    Maps(int mapID, Map map) {
    }
}
